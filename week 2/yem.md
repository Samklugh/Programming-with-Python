### STRING METHODS IN PYTHON
**
    1. .upper()
    2. .lower()
    3. .Capitalize()
    4. .title()
    5. .casefold()        **converts to lowercase*
    6. .center(100)       **align items to center using assigned argument*
    7. .replace("sam", "mercy") **finds sam and replace it with mercy*
    8. .count("sam")   **Returns the number of times sam occured*
    9. .strip()    **Removes the empty space before and at the end*
    10. .startswith("s")   **checks if variable starts with "s". T/F*
    11. .endswith("son")   **checks if variable ends with "son". T/F*
    12. .find("samson")   **finds a search adn return its index/position*
    13. .format(name="Yemi")    **used for string formating*
    14. .index("s")     **gives the index of the search*
    15. .join(what to join)----- **"sep".join(tuple)---changes to string*
    16. .partition("banana")   **returns a tuple*
    17. .swapcase()     **swaps font case style*
    18. .split()  **Splits the string at the specified separator, and returns a list*
    19. zfill(5)  *** Fills the string with a specified number of 0 values at the beginning*
    20. isalpha()
    21. isalnum()
    22. isascii()
    23. isdecimal() *Check if all the characters in a string are decimals (0-9)*
    24. isdigit() *Check if all the characters in the text are digits:*
    25. isprintable() *Check if all the characters in the text are printable:*
    26. isspace() *Check if all the characters in the text are whitespaces:*
    27. istitle()
    28. isupper()
    29. islower()
    30. isnumeric()
    31. ljust() *left justify*
    32. rjust()
    33. .splitlines()   *Split a string into a list where each line is a list item:*
    34. .expandtabs(2)  *(txt = "H\te\tl\tl\to" and x =  txt.expandtabs(2))*
    35. 


   